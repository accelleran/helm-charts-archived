apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "five-g-appl.fullname" . }}-sctp
  labels:
    {{- include "five-g-appl.labels" . | nindent 4 }}
spec:
{{- if not .Values.e2tsctp.autoscaling.enabled }}
  replicas: {{ .Values.e2tsctp.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "five-g-appl.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- with .Values.e2tsctp.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "five-g-appl.labels" . | nindent 8 }}
    spec:
      {{- with .Values.e2tsctp.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "five-g-appl.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.e2tsctp.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.e2tsctp.securityContext | nindent 12 }}
          image: "{{ .Values.e2tsctp.image.repository }}:{{ tpl .Values.e2tsctp.image.tag . | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.e2tsctp.image.pullPolicy }}
          {{ if .Values.e2tsctp.developerMode.enabled }}
          command:
            - sh
            - "-c"
            - |
              /bin/bash <<'EOF'
              while true; do sleep 30; done;
              EOF
          {{ else }}
          command:
            - sh
            - "-c"
            - |
              /bin/sh <<'EOF'
              {{ .Values.e2tsctp.cmd }} {{- if .Values.e2tsctp.args }}{{ range .Values.e2tsctp.args }} {{ .name }} {{ .value }}{{ end }}{{- end }}
              EOF
          {{ end }}
          env: 
            - name: NATS_SERVICE_URL
              value: nats://{{ tpl .Values.e2tsctp.nats.url . }}:{{ tpl .Values.e2tsctp.nats.port . }}
            - name: ZLOG_CONF_PATH
              value: /home/accelleran/5G/zlog.conf
            - name: BOOTSTRAP_FILENAME
              value: {{ .Values.e2tsctp.bootstrapFile }}
            - name: JAEGER_AGENT_HOST
              value: {{ tpl .Values.e2tsctp.jaegerAgentHost . }}
            - name: __APPNAME
              value: sctpE2ap
            {{- if .Values.e2tsctp.extraEnvs }}
            {{ toYaml .Values.e2tsctp.extraEnvs | nindent 12 }}
            {{- end }}
          ports:
            - name: {{ tpl .Values.e2tsctp.sctpService.name . }}
              containerPort: {{ .Values.e2tsctp.sctpService.port}}
              protocol: {{ .Values.e2tsctp.sctpService.protocol }}
#          livenessProbe:
#            httpGet:
#              path: /
#              port: http
#          readinessProbe:
#            httpGet:
#              path: /
#              port: http
          volumeMounts:
            - name: {{ include "five-g-appl.fullname" . }}-zlog-conf
              mountPath: /home/accelleran/5G/zlog.conf
              subPath: "zlog.conf"
            - name: {{ include "five-g-appl.fullname" . }}-bootstrap
              mountPath: {{ .Values.e2tsctp.bootstrapFile }}
              subPath: "bootstrap.txt"
            {{ if and (.Values.e2tsctp.developerMode.enabled) (.Values.e2tsctp.developerMode.hostPath) }}
            - name: dev-work-dir
              mountPath: /home/accelleran/5G/dev/
            {{ end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          lifecycle:
            preStop:
              exec:
                command: [ '/bin/sh', '-c', 'pkill -15 -e {{ .Values.e2tsctp.cmd | trimPrefix "./" | trimSuffix ".exe" | trunc 15  }}' ]
      {{- if or (.Values.e2tsctp.nodeSelector) (eq (tpl .Values.e2tsctp.draxNodeSelectorEnabled .) "true") }}
      nodeSelector:
        {{ if .Values.e2tsctp.nodeSelector }}
          {{- toYaml .Values.e2tsctp.nodeSelector | nindent 8 }}
        {{ end }}
        {{ if eq (tpl .Values.e2tsctp.draxNodeSelectorEnabled .) "true" }}
          {{- tpl (toYaml .Values.e2tsctp.draxNodeSelector) $ | nindent 8 }}
        {{ end }}
      {{- end }}
      {{- with .Values.e2tsctp.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.e2tsctp.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: {{ include "five-g-appl.fullname" . }}-zlog-conf
          configMap:
            name: {{ include "five-g-appl.fullname" . }}-zlog-conf

        - name: {{ include "five-g-appl.fullname" . }}-bootstrap
          configMap:
            name: {{ include "five-g-appl.fullname" . }}-bootstrap

        {{ if and (.Values.e2tsctp.developerMode.enabled) (.Values.e2tsctp.developerMode.hostPath) }}
        - name: dev-work-dir
          hostPath:
            path: {{ .Values.e2tsctp.developerMode.hostPath }}
        {{ end }}
